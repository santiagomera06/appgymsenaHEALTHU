// lib/screens/clasificacion_screen.dart

import 'package:flutter/material.dart';

/// Por ahora dejamos esta pantalla en blanco:
class ClasificacionScreen extends StatelessWidget {
  const ClasificacionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      // Opcion: podrías poner un Center(child: Text("Clasificación va aquí"));
      body: SizedBox.shrink(),
    );
  }
}
